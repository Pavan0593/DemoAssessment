==========================================================================
Instruction to run the test suite
==========================================================================

1. Go to home directory of the cloned source
2. Edit SetUp.sh file to point to the web driver location
3. Run the shell script as shown below
  {
   . SetUp.sh
  }
